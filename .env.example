# Local development with `dotnet run` (postgres exposed on host port 5432)
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=bareq_alnaqool;Username=postgres;Password=postgres

# When running API containers via docker-compose, use Host=postgres instead of localhost.
# ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=bareq_alnaqool;Username=postgres;Password=postgres

# JWT (override in production — use a long random secret)
Jwt__Key=BareqAlNaqoolDevSecretKeyChangeInProduction!
Jwt__Issuer=BareqAlNaqool.Api
Jwt__Audience=BareqAlNaqool.Clients
Jwt__ExpiryMinutes=1440

# Railway sets these automatically when you add a Postgres plugin:
# DATABASE_URL=postgresql://user:pass@host:port/railway
# PORT=8080
